name: CICD Engine
run-name: CICD Engine for ${{ vars.APP_NAME }} by @${{ github.actor }}
on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]
  workflow_dispatch:



jobs:
  # deploy-networking:
  #   permissions:
  #     contents: read
  #     id-token: write # Required for Workload Identity Federation
  #   uses: './.github/workflows/networking.yaml'
  #   with:
  #     environment: ${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}
  #   secrets: inherit

  deploy-data:
    # needs: [deploy-networking]
    permissions:
      contents: read
      id-token: write # Required for Workload Identity Federation
    uses: './.github/workflows/data.yaml'
    with:
      environment: ${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}
    secrets: inherit

  check-output:
    runs-on: ubuntu-latest
    needs: [deploy-data]
    permissions:
      contents: read
      id-token: write # Required for Workload Identity Federation
    steps:
        - run: echo "Result From Data - ${{ needs.deploy-data.outputs.gcp_container_repo_uri }}"

  # deploy-app:
  #   needs: [deploy-data]
  #   permissions:
  #     contents: read
  #     id-token: write # Required for Workload Identity Federation
  #   uses: './.github/workflows/app.yaml'
  #   with:
  #     environment: ${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}
  #   secrets: inherit

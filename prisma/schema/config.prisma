generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder", "multiSchema"]
}

datasource db {
  provider = "mongodb"
  url      = env("LOCAL_MONGO_CONNECTION_URL")
}
